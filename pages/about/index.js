import Layout from '../../components/Layout';

const menuItem = 'about';

const Index = () => (
    <Layout title="О нас" menuItem={menuItem}>
        <h1>Мы такие-то такие-то</h1>
    </Layout>
);

export default Index;
